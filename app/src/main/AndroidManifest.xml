<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ISTJobs"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".UserDashboardActivity" />
        <activity android:name=".AdminDashboardActivity" />
<!--        <activity android:name=".PostJobsActivity" />-->
        <activity android:name=".CandidatesActivity" />
        <activity android:name=".AdminProfileActivity" />
        <activity android:name=".AdminSignUpActivity" />

        <!-- Declare User Sign Up and Login Activities -->
        <activity android:name=".UserSignUpActivity"/>
        <activity android:name=".UserLoginActivity"/>

        <!-- Declare Admin Login Activity -->
        <activity android:name=".AdminLoginActivity"/>

        <!-- Declare User Profile, Notifications, and Search Jobs Activities -->
        <activity android:name=".UserProfileActivity"/>
        <activity android:name=".NotificationsActivity"/>
        <activity android:name=".SearchJobsActivity"/>

    </application>

</manifest>